// The /admin/login route — the admin sign-in, separate from the customer one.

import React from "react";
import DsAdminLogin from "../ds/DsAdminLogin.jsx";

export default function AdminLogin() {
  return <DsAdminLogin />;
}
