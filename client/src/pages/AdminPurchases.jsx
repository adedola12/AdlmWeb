// The /admin/pending route — his Purchases queue.
//
// Named "pending" because that is the path the old hub already used and the
// one people have bookmarked; the rail calls it Purchases, as he does.

import React from "react";
import DsAdminPurchases from "../ds/DsAdminPurchases.jsx";

export default function AdminPurchases() {
  return <DsAdminPurchases />;
}
