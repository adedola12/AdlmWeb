// The /admin/youtube route — the channel beside the library, in his design.
//
// pages/AdminYoutubeStatus.jsx is the same screen in the older admin build
// and stays in the tree for it.

import React from "react";
import DsAdminYoutube from "../ds/DsAdminYoutube.jsx";

export default function AdminYoutube() {
  return <DsAdminYoutube />;
}
