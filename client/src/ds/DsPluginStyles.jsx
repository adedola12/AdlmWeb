// The plugin side-one sheet (his plugin-quiv.css, 17 Sep), for the staged
// /preview/plugin-quiv and /preview/plugin-heron design references. A
// component rather than an import in the generated pages, for the same
// reason as DsBeyondBimStyles.
import "../styles/ds-plugin.css";

export default function DsPluginStyles() {
  return null;
}
