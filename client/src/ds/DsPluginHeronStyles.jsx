// His HERON plugin sheet (assets/css/plugin-heron.css, 22 Sep). It replaced
// plugin-quiv.css on that page when he rebuilt HERON on how it actually works,
// so the two plugin references no longer share one stylesheet.
import "../styles/ds-plugin-heron.css";

export default function DsPluginHeronStyles() {
  return null;
}
